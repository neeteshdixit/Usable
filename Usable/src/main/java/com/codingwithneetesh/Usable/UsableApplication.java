package com.codingwithneetesh.Usable;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class UsableApplication {

	public static void main(String[] args) {

        SpringApplication.run(UsableApplication.class, args);
	}

}
// rest api => representation state transfer (REST) aplication programming interface (API) is a set of rules that allows programs to talk to each other.